import { useLocation } from "react-router"

export const UpdatedDataCategory = () => {
    const location=useLocation();

    return(
        <div>
            <form>
                <input
                    name=""
                />
            </form>
        </div>
    );
}